spring:
  datasource:
    url: jdbc:h2:mem:gestionstock
    username: root
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true